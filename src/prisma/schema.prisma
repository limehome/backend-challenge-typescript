datasource db {
  provider = "sqlite"
  url      = "file:../../data/app.db"
}

generator client {
  provider = "prisma-client-js"
}

model Booking {
  id             Int     @id @default(autoincrement())
  guestName      String
  unitID         String
  checkInDate    DateTime
  numberOfNights Int
}
